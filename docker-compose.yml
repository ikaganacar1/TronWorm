version: '3.8'

services:
  tronworm:
    build:
      context: .
      dockerfile: Dockerfile
    image: tronworm:latest
    container_name: tronworm-game
    ports:
      - "8081:8081"  # HTTP server
      - "8766:8766"  # WebSocket server
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.socket(); s.connect(('localhost', 8081)); s.close()"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - tronworm-net

networks:
  tronworm-net:
    driver: bridge
